# CampusConnect
A program that scrapes event data from the Adrian College Calendar webpage, cleans the returned data, and stores the results in a SQLite database. To add to this functionality, a public authenticated weather API was utilized to retrieve, validate, and store current weather information for the campus’s location in a SQLite database.
